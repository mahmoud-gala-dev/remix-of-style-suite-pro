# Audit Cycle #12 — 2026-06-22

## Focus
Lock-in cycle #11's UUID seed fix with a regression test (debt #4 — expand coverage).

## Changes
- `tests/seed-uuids.test.ts` (new): asserts every seed entity id is RFC-4122 UUID and all cross-references resolve.
- `src/lib/seed.ts`: `rid()` now uses `crypto.randomUUID()` (with safe fallback), so queue ids are also UUIDs.

## Verification
`bunx vitest run tests/seed-uuids.test.ts` → 7/7 passed.

## Score Protection
No behavior changes — purely defensive. Debt #15 remains closed and is now protected by a unit test.

## Next
Cycle #13 candidates: add server-fn integration tests for shifts/waitlist, or address debt #9 (`any` types) if a typed Supabase table-routing helper can be introduced.