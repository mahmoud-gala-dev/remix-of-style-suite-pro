# Audit — 2026-06-22 (cycle #1)

## Previous Score
Baseline (no prior audit).

## New Score
**Overall 7.9 / 10** (see `current-score.md`).

## Improvements Implemented
- **Security P1**: rate-limit on `requestOtp` and `verifyOtp` (5 per 10min / 10 per min per phone).
- **Security P1**: stop returning the OTP code in the API response unless `NODE_ENV !== "production"` or `EXPOSE_OTP_FOR_TESTS=1`.
- **Security P1**: `/api/public/cron/process-webhooks` and `/api/public/cron/retry-webhooks` now require header `x-cron-secret: $CRON_SECRET` whenever that env is set (still open in dev when unset).
- **Maintainability**: extracted shared `requireAdmin(ctx)` helper at `src/lib/require-admin.ts` and migrated `otp.functions.ts` to it (others to be backfilled).
- **Memory**: created `docs/project-memory/` (overview, current-score, technical-debt, backlog, architecture-decisions, features, known-issues, progress).

## Files Modified
| File | Reason |
|---|---|
| `src/lib/otp.functions.ts` | Added rate limiting + redacted OTP in prod + switched to shared admin gate |
| `src/routes/api/public/cron/process-webhooks.ts` | Added `x-cron-secret` auth |
| `src/routes/api/public/cron/retry-webhooks.ts` | Added `x-cron-secret` auth |

## Files Created
| File | Reason |
|---|---|
| `src/lib/require-admin.ts` | Shared admin gate (eliminates 5–6 duplicate boilerplate blocks) |
| `docs/project-memory/*` | Long-term audit memory (per AUDIT MEMORY SYSTEM directive) |

## Files Deleted
None.

## Bugs Fixed
- OTP brute-force possibility.
- OTP code disclosure in prod responses.
- Anyone could trigger webhook retry storms via cron endpoints.

## Features Added
No new user-visible features (audit cycle).

## Technical Debt Remaining
See `technical-debt.md`.

## Risks
- Twilio still not wired to OTP send — production OTP requires `EXPOSE_OTP_FOR_TESTS=1` or the user cannot receive the code.
- VAPID private key fallback in source remains until production envs are confirmed.

## Next Priorities
- **P1**: wire Twilio → OTP send; remove VAPID hardcoded fallback in prod.
- **P2**: add `<DataState>` to the 11 admin pages; booking-confirmation email; customer invoice PDF; realtime for queue/calendar; lazy-load reports/calendar.
- **P3**: per-employee shifts; recurring-booking UI; expand test coverage.
- **P4**: PDF lib consolidation; theme tokenization for WhatsApp brand color.

## Production Readiness
**Now**: 78%. **Was**: ~72% (estimated baseline before this cycle).

## Did we reach 10/10?
No. Blockers: incomplete loading/error states across admin pages, no realtime, no lazy-loading, OTP-via-SMS not wired, hardcoded VAPID fallback, low automated test coverage.