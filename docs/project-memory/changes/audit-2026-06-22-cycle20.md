# Audit Cycle #20 — 2026-06-22 — Roadmap reconciliation

## Focus
Verify the prioritized backlog (Drag-Drop Calendar, Realtime, Shifts UI, WhatsApp, AI No-Show) against actual code state.

## Findings
| Item | Status | Evidence |
|---|---|---|
| Drag-Drop Calendar | **Already shipped** | `src/routes/_authenticated/calendar.tsx` — HTML5 drag/drop, `rescheduleBooking`, optimistic rollback, toast on conflict |
| Realtime Sync | **Shipped cycle #19** | `bookings`/`queue_items`/`invoices`/`payments` in publication, kiosk subscribes per-branch |
| Shifts UI | **Already shipped** | `src/routes/_authenticated/shifts.tsx` — full CRUD weekly shifts + days-off |
| AI No-Show Predictor | **Already shipped** | `predictNoShow` (deterministic risk 0-100 + level), `AiSuggestionsPanel`, `holdBookingDeposit`/`captureBookingDeposit`/`releaseBookingDeposit` via Stripe BYOK |
| WhatsApp Business API | **Blocked on Twilio connector** | `twilio.functions.ts` wired; needs workspace Twilio connection + verified WhatsApp sender |

## Doc updates
- `known-issues.md`: KI-006 marked Resolved.
- KI-003 already resolved in cycle #19.

## Score impact
No code change. Removes phantom backlog items that were already done. Real outstanding work: WhatsApp connector + UI for sending templated messages.

## Next
- Connect Twilio (workspace-level) → wire `sendWhatsApp` button on booking row for confirm/reminder/reschedule.
- Then cycle #21: marketing campaigns built on the WhatsApp + notification queue.