# Cycle #28 — Inventory Auto-Reorder

- New tables: `suppliers`, `purchase_orders`, `purchase_order_items` (branch-scoped RLS).
- `products` extended with `reorder_point`, `reorder_qty`, `supplier_id`, `last_auto_po_at`.
- Server fns in `src/lib/purchasing.functions.ts`:
  - Suppliers: `listSuppliers`, `upsertSupplier`, `deleteSupplier`.
  - Reorder: `updateProductReorder`, `listLowStockProducts`, `runAutoReorderScan`
    (groups low-stock products by supplier → one draft PO each; 24h dedupe via `last_auto_po_at`).
  - POs: `listPurchaseOrders`, `getPurchaseOrder`, `createPurchaseOrder`, `updatePOStatus`
    (on `received` → stock incremented + `stock_movements` row with `kind='purchase'`).
- New route `/_authenticated/purchasing.tsx` — low-stock table, supplier CRUD,
  PO list with status workflow, one-click Auto-reorder scan.
- Sidebar: "Purchasing" under management group with truck icon.