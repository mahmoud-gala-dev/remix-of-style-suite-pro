# Audit Cycle #14 — 2026-06-22

## Focus
Close debt #9 at the call sites by routing dynamic Supabase inserts through a single typed helper.

## Changes
- `src/lib/dynamic-table.server.ts` (new): `insertDynamic(client, table, rows)` — the one place where the runtime-table `any` cast lives, with a doc-comment explaining why Supabase's typed `from()` can't accept dynamic strings.
- `src/lib/admin.functions.ts`: `seedDemo` insert helper now delegates to `insertDynamic` (no local `any`).
- `src/lib/restore.functions.ts`: per-section row insert now uses `insertDynamic` (hoisted dynamic import out of the loop).
- `docs/project-memory/technical-debt.md`: debt #9 reduced to a single contained escape hatch.

## Verification
`bunx tsc --noEmit` → clean.

## Score Protection
No runtime change; surface of `any` shrunk from N call sites to 1 documented helper.

## Next
Cycle #15 candidates: integration test for `shifts.functions.ts` overlap rules, or audit `web-vitals` shim for a typed wrapper.