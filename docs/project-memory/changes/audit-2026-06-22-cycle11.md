# Audit Cycle #11 — 2026-06-22

## Focus
Permanently close debt #15 (non-UUID seed IDs) by minting stable UUIDs at the source.

## Changes
- `src/lib/seed.ts`: replaced string IDs (`br-downtown`, `em1`, `cu1`, `sv1`, `bk1`) with stable RFC-4122 UUIDs via `BR_*` constants and `SV(n)`/`EM(n)`/`CU(n)`/`BK(n)` helpers. Cross-references (branchId/customerId/employeeId/serviceId) updated in lockstep.
- `src/routes/_authenticated/shifts.tsx`: reverted the cycle-#9 local UUID filter — no longer needed now that seed employees have valid UUIDs.
- `docs/project-memory/technical-debt.md`: marked debt #15 as fixed.

## No external references
`rg` confirmed no other source file hardcoded the old string IDs, so the swap is safe.

## Score Protection
No regressions: all server fns that validate `z.string().uuid()` now accept seed data uniformly.

## Next
Cycle #12 candidates: debt #9 (remaining `any` types in admin/restore — intentional generics) and #4 (test-coverage expansion).