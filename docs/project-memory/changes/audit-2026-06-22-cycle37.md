# Cycle #37 — Public REST API v1

Shipped tenant-scoped read API (`/api/public/v1/{branches,services,bookings}`) with hashed API keys, per-key rate limiting, scopes (read/write), one-time key reveal, admin UI at `/api-keys`, and OpenAPI 3.0.3 spec at `/api/public/v1/openapi/json`.

## Score delta
Overall 8.3 → **8.4** / 10. Large Business 90% → **95%**.

## Files
- New: `supabase/migrations/...api_keys.sql`, `src/lib/api-key-auth.server.ts`, `src/lib/api-keys.functions.ts`, `src/routes/api/public/v1/{branches,services,bookings,openapi.json}.ts`, `src/routes/_authenticated/api-keys.tsx`.
- Edited: `src/lib/i18n.ts` (+13 keys), `src/lib/modules.ts` (KeyRound + `/api-keys`), `src/routeTree.gen.ts` (regenerated).

## Next
P3 write endpoints (reuse `createBooking`), per-key usage counters, webhook subscriptions.